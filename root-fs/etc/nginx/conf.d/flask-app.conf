# INFRA HEADER -- DO NOT EDIT. metadata: {"checksum": "3ccae3e56b3faba350eb30689e3ec3d5"}
server {
    location / {
        try_files $uri @app;
    }

    location @app {
        include uwsgi_params;
        uwsgi_pass unix:/var/run/flask-app.sock;
    }
}