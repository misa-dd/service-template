version: '2'
service:
  containers:
    app:
      command: /bin/bash -c 'while sleep 5; do echo 123; done'
      build: .
      is_make_target: true
      development:
        volumes:
          - $PWD:/home/app
#        rsync:
#          - $PWD:/home/app
  localdev:
    envdefaults:
      - COLOR=blue
    envfile: testvars.env
